import { MindTargetWorkbench } from "@/components/workbench/MindTargetWorkbench";

export const metadata = {
  title: "Artify AR Workbench",
  description: "Compile MindAR image targets for Artify WebAR artworks.",
};

export default function WorkbenchPage() {
  return <MindTargetWorkbench />;
}
