# Artify Landing Page

Standalone first-screen landing experiment for Artify.

- Left side links to `/social`
- Right side links to `/ar`
- Uses local image assets from `assets/`
- Uses GSAP from CDN for intro, floating photos, and pointer movement

Open `index.html` directly in a browser, or serve this folder with any static server.
